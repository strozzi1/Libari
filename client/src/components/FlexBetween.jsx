import { Box } from "@mui/material";
import { styled } from "@mui/system";

//super useful, gonna use a lot
const FlexBetween = styled(Box)({
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
})

export default FlexBetween;